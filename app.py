import streamlit as st
# ... (import อื่นๆ เหมือนเดิม)

# 1. ตั้งค่าหน้าเว็บ (ส่วนนี้วางไว้บนสุดได้เลย)
st.set_page_config(page_title="Photo Finder System", layout="wide")

# 2. ย้าย auto_sync_gdrive() ไปไว้ล่างๆ หรือใช้การเรียกเมื่อจำเป็น
# แทนที่จะให้มันรันทันทีที่เริ่มโปรแกรม ให้ใช้สภาวะเงื่อนไข หรือวางไว้ท้ายสุด
# เพื่อให้ Streamlit โหลดปุ่มขึ้นมาแสดงให้ผู้ใช้เห็นก่อน

# --- UI หลัก (สร้างปุ่มไว้ที่นี่ก่อน) ---
st.title("📸 ระบบสแกนใบหน้าค้นหารูปถ่ายในงาน")

# ... (ส่วนเมนู Sidebar ของคุณ) ...

if choice == "หน้าหลักสำหรับสแกนรูป":
    # ปุ่มอัปโหลดจะแสดงผลทันที เพราะอยู่ต้นๆ ของส่วนนี้
    uploaded_file = st.file_uploader("อัปโหลดรูปภาพใบหน้าของคุณ...", ...)
    
    if uploaded_file:
        # ... (โค้ดสแกนใบหน้า) ...

# --- การดึงข้อมูล (ย้ายมาวางไว้ท้ายสุดของไฟล์) ---
# วิธีนี้ทำให้โค้ดส่วน UI ด้านบนถูกโหลดเสร็จก่อนที่ระบบจะไปดึงรูป
auto_sync_gdrive()
